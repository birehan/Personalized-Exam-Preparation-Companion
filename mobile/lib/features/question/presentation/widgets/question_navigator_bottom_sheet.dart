import 'package:flutter/material.dart';

class QuestionNavigatorBottomSheet extends StatelessWidget {
  const QuestionNavigatorBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}