enum FeedbackType { questionFeedback, contentFeedback }

enum QuestionMode { learning, quiz, analysis, none }

enum ExamType { quiz, standardMock, aiGeneratedMock, endOfChapterQuestions, downloadedMock }

enum MockType {
  recommendedMocks,
  standardMocks,
  myStandardMocks,
  myAIGeneratedMocks,
  bookmarkedQuestion,
  downloadedMock,
}
